import { useState, useEffect } from "react";

export default function App() {
  // 1. Create state for dogs and loading

  // 2. Fetch the list of dog breeds from https://dog.ceo/api/breeds/list/all
  //    and update state when the data is loaded

  return (
    <div>
      <h1>Dog Explorer</h1>
      {/* 3. Show "Loading..." while loading, otherwise show the list of breeds */}
    </div>
  );
}
